import { createSlice } from '@reduxjs/toolkit';

export const glassesSlice = createSlice({
    name: 'glasses',
    initialState: {
        glasses:{},
        errorMessage: undefined
    },
    reducers: {

    }
});

export const {  } = glassesSlice.actions;